import styles from "./default.module.scss";
import { ReactElement } from "react";

export default function ProductsSidebar(): ReactElement {
	return (
		<aside className={styles.aside}></aside>
	);
}
